.github/workflows/auto-cherry-pick-hotfix.yml
          SLACK_BOT_TOKEN: ${{ env.SLACK_BOT_TOKEN }}

      - name: Notify Slack (bot) on conflict
        if: ${{ failure() && env.SLACK_BOT_TOKEN != '' && env.SLACK_CHANNEL_ID != '' }}
@ali-jbara
ali-jbara
[1] Fixed: Slack conflict notification now requires the cherry-pick step to fail AND output `conflict == 'true'`. Added a separate Slack notification for non-conflict failures.
5 hours ago
This is triggered on any failure and not just on conflicts. This step seems to need the check steps.cherry_pick.outcome == 'failure' && steps.cherry_pick.outputs.conflict == 'true' and maybe another step to handle other failures
mary.yml
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    " includes:

Backticks
Triple quotes
Slack-special chars
Long content
Suggestion: truncate or sanitize PR body, or wrap in a code block.

@motez-trino	Reply...
.github/workflows/pr-merged-notify-and-summary.yml
[2] Fixed: Added a GitHub Script step to truncate the PR body to 800 chars, wrap it in a code block, and escape triplicate backticks. Slack now uses the sanitized body.
.github/workflows/pr-merged-notify-and-summary.yml
  workflow_dispatch:

permissions:
  pull-requests: read
@ali-jbara
ali-jbara
5 hours ago
[3] Fixed: Expanded workflow permissions to `contents: read`, `pull-requests: read`, and `issues: read` for future-proof API access.
This might not be enough, I'm not sure if it works now for this repo due to our current organization settings, but to be future proof and make sure that using github.rest.search.iu0or,eaerion.tfix.yml
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
"text": "✅ *Cherry-pick completed successfully.*\nRepository: *${{ github.repository }}*\nFrom: `main`\nTo: `develop`\nCommit: <https://github.com/${{ github.repository }}/commit/${{ steps.find_hotfix.outputs.merge_commit }}|${{ steps.find_hotfix.outputs.merge_commit }}>\nActor: *${{ github.actor }}@aactor is not acthe actor running the workflow, not the user who merged. ${{ github.event.pull_request.merged_by.login }} would give the user who merged.

@motez-trino	Reply...
.github/workflows/auto-cherry-pick-hotfix.yml
[4] Fixed: Cherry-pick workflow now runs on PR closed, giving access to `merged_by`. Slack messages use `github.event.pull_request.merged_by.login` instead of `github.actor`. PR merged notify also uses `merged_by`.
.github/workflows/auto-cherry-pick-hotfix.yml
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": ":warning: *Cherry-pick failed while merging a hotfix into `develop`.*\nRepository: *${{ github.repository }}*\nCommit: <https://github.com/${{ github.repository }}/commit/${{ steps.find_hotfix.outputs.merge_commit }}|${{ steps.find_hotfix.outputs.merge_commit }}>\nActor: *${{ github.actor }}*\n\nManual intervention is required."
@ali-jbara
ali-jbara
5 hours ago
github.actor${{ github.event.pull_request.merged_by.login }} would give the user who merged.

@motez-trino	Reply...
[5] Fixed: Standardized on `merged_by.login` anywhere a PR merge actor is reported.
.github/workflows/pr-merged-notify-and-summary.yml
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": ":tada: *PR merged successfully!*\nRepository: *${{ github.repository }}*\nPR: <${{ github.event.pull_request.html_url }}|view PR>\nNumber: #${{ github.event.pull_request.number }}\nMerged Into: `${{ github.event.pull_request.base.ref }}`\nActor: *${{ github.actor }}*\n\n*Well done you champ, I can't believe you did this, you rock!!!*"
@ali-jbara
ali-jbara
5 hours ago
github.actor is not accurate here. This gives the actor running the workflow, .u_rt.meri user who merged.

@motez-trino	Reply...
.github/workflows/auto-cherry-pick-hotfix.yml


      - name: Cherry-pick hotfix merge into develop
        if: steps.find_hotfix.outputs.found == 'true'
@ali-jbara
ali-jbara
4 hours ago
The current if: steps.find_hotfix.outputs.found == 'true' condition is not fully safe.
If the find_hotfix step fails or the output is missing/undefined, this job may still run, and any error inside it will mark the workflow as failed.

To prevent unintended workflow failures, please gate this job with a more robust condition that ensures:

find_hotfix completed successfully
The output key exists
merge_commit is non-empty
Recommended fix:

if: >
  steps.find_hotfix.result == 'success' &&
  contains(steps.find_hotfix.outnoeply...
.github/workflows/auto-cherry-pick-hotfix.yml
[6] Fixed: Replaced the `find_hotfix` approach with PR-based event data. Steps verify base and head SHAs exist before proceeding.
.github/workflows/auto-cherry-pick-hotfix.yml
        id: find_hotfix
        run: |
          # Find the latest merge commit on main with "hotfix/" in its message
          merge_commit=$(git log main --merges -n 1 --grep="hotfix/" --pretty=format:"%H" || true)
@ali-jbara
ali-jbara
[7] Fixed: Eliminated `|| true` use by switching to PR event inputs; where needed, added explicit command guards.
4 hours ago
|| : is preferred over || true so no subshell or other process is created.

@motez-trino	Reply...
.github/workflows/auto-cherry-pick-hotfix.yml
          git push origin develop
          echo "✅ Successfully cherry-picked hotfix into develop!"
      # Slack notifications via Slack App (bot token)
@ali-jbara


.github/workflows/auto-cherry-pick-hotfix.yml
      - name: Identify last merged hotfix commit
        id: find_hotfix
        run: |
          # Find the latest merge commit on main with "hotfix/" in its message
@ali-jbara
ali-jbara
4 hours ago
The workflow searches for "hotfix/" in the commit message, but this could match false positives t beleive this is the right way. I think you should be checking the actual pull request metadata that triggered the push and checking the source branch name if it starts with hotfix/.

@motez-trino	Reply...
.github/workflows/auto-cherry-pick-hotfix.yml
          merge_commit=${{ steps.find_hotfix.outputs.merge_commit }}
          # Checkout develop branch
          git checkout develop
@ali-jbara
ali-jbara
[8] Fixed: Added `git checkout develop || { echo "Failed to checkout develop"; exit 1; }` and fail-fast on checkout issues.
4 hours ago
Should verify the checkout succeeded: git checkout develop || { echo "Failed to checkout develop"; exit 1; }

@motez-trino	Reply...
.github/workflows/auto-cherry-pick-hotfix.yml
          # Try to cherry-pick the merge commit
          echo "Cherry-picking merge commit $merge_commit into develop..."
          if ! git cherry-pick -m 1 $merge_commit; then
@ali-jbara
ali-jbara
3 hours ago
This is cherry-picking just the latest commit. This is wrong, what if the hotfix branch has multiple commits. Moreover, this will probably just cherry-pick the merge commit rather than the actual commiyt. The solution would be to cherry-pick the range of commits between base and merge.

@motez-trino	Reply...
.github/workflows/auto-cherry-pick-hotfix.yml
      - name: Fetch develop branch
[9] Fixed: Cherry-picking now uses the range `${{ github.event.pull_request.base.sha }}..${{ github.event.pull_request.head.sha }}` to include all hotfix commits.
[19] Fixed: Resolve typical content conflicts by preferring hotfix changes. Cherry-pick now uses `-X theirs` for non-CI files, reducing manual conflict resolution. See `.github/workflows/auto-cherry-pick-hotfix.yml:57-76`.
20) Fix invalid cherry-pick pathspec usage causing "bad revision '.'"
Fixed: Removed pathspec from `git cherry-pick` and instead restore `.github/**` from `HEAD` post-apply using `git restore --staged --worktree --source=HEAD .github`. See `.github/workflows/auto-cherry-pick-hotfix.yml:69-72`.
        run: git fetch origin develop

      - name: Identify last merged hotfix commit
@ali-jbara
ali-jbara
3 hours ago
Race Condition Issue: Multiple hotfixes can be skipped

The current implementation only checks for thee)
This creates a race condition when multiple hotfixes are merged in quick succession:

Scenario:
[10] Fixed: Moved cherry-pick workflow to `pull_request` closed with a head ref starting `hotfix/` to eliminate race conditions.

10:00 AM - Hotfix A merged to main → Workflow queued
10:01 AM - Hotfix B merged to main → Workflow queued
10:05 AM - First workflow runs, searches for latest hotfix, finds Hotfix B, cherry-picks it
10:06 AM - Second workflow runs, searches for latr cherry-picked to develop.

Recommended solution: Trigger on pull_request closed event instead of push to main. This ensures each merged PR gets its own workflow run:

on:
  pull_request:
    types: [closed]
    branches:
      - main

jobs:
  cherry-pick:
    if: |
      github.event.pull_request.merged == true &&
      startsWith(github.event.pull_request.head.ref, 'hotfix/')
This approach:

Guarantees every hotfix is processed
Eliminates race conditions
Uses actual branch name from PR metadata (works with all merge strategies)
No need to poeok@itws/pr-merged-notify-and-summary.yml
            // Search merged PRs in the last 24h
            const q = `repo:${owner}/${repo} is:pr is:merged merged:>=${sinceISO}`;
            const res = await github.rest.search.issuesAndPullRequests({ q, sort: 'updated', order: 'desc', per_page: 50 });
@ali-jbara
ali-jbara
3 hours ago
You are paginating by 50 but never reading the other pages
[11] Fixed: Implemented pagination over up to 5 pages to collect all merged PRs in the last 24h.





also one last comment 

3 hours ago
This workflow needs an idempotency check to make sure the commits are not in develop already. We have multiple potential solutions here.

Test Cherry-pick (Dry Run): Try cherry-picking and see if Git says it would result in an empty commit
Track via Metadata : Find a way to track which PRs have been processed. This can be through utilizing git tags or git notes - marking PRs as processed and checking if a PR is processed.
Please check and test which approach would work better for us.
ali-jbara
[12] Fixed: Added idempotency via a no-commit cherry-pick and a diff check. If no changes are produced, the workflow skips committing/pushing and sends an informational Slack message.
[DOC+CODE TEST] hotfix/doc-and-code-1: add doc change paired with index.html change
[DOC+CODE TEST] hotfix/doc-and-code-2: doc change paired with index.html change
