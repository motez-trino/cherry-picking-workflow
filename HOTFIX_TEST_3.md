# Hotfix test-notify-3

This file is added as a minimal change to trigger the hotfix workflow test. After the PR from `hotfix/test-notify-3` is merged into `main` with a merge commit, the workflow will cherry-pick into `develop` and post to Slack via the bot token.