# Hotfix test-notify-6

Final minimal change to trigger the hotfix workflow. After PR from `hotfix/test-notify-6` is merged into `main` with a merge commit (title includes `hotfix/`), it should cherry-pick to `develop` and post to Slack.