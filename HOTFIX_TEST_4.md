# Hotfix test-notify-4

Minimal change for another hotfix workflow test. After the PR from `hotfix/test-notify-4` is merged into `main` with a merge commit (title includes `hotfix/`), the workflow should cherry-pick into `develop` and post to Slack.